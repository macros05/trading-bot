# Secrets and credentials
.env

# Bytecode and venvs
__pycache__/
*.pyc
*.pyo
venv/

# Runtime state files (managed by the bot, reconciled with the exchange on start)
bot_state.json
bot_health.json
trades_history.json
data/bot_state.json
data/bot_health.json
data/trades_history.json
data/*.db

# Runtime logs (rotated locally)
*.log
*.log.[0-9]*
*.log.[0-9]*.gz
logs/

# Backtest binary caches (regenerable from raw OHLCV)
backtest/results/_cache_*.pkl

# One-off audit / scan outputs
audit-report.json
